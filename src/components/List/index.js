// style
import styled from 'styled-components';

const StyledBaseContents = styled.div`
  display: flex;
  flex-direction: row;
  justify-content: center;
  align-items: center;
`;

const List = () => {
  return <StyledBaseContents></StyledBaseContents>;
};

export default List;
